# AI SOP Platform 部署與服務啟動教學說明 (Vanilla ZERO-Install 版)

這份說明將指引你如何在另一台全新電腦（例如公司內有嚴苛網路與套件限制的設備）完整建置並啟動這個網頁應用系統。

本分支(`vanilla-frontend`)專為有嚴格限制的公司環境打造，**完全不依賴 Node.js, npm, 或前端現代框架**。我們直接將原生的 HTML / JS 作為靜態檔案提供。

---

## 🛠️ 事前準備：環境必備需求

由於前端不需要任何編譯，你**只需要準備後端的系統環境**：
1. **Python (推薦 3.10 或以上)**

---

## 🚀 部署與啟動步驟

只要你在公司新電腦成功取得此專案檔案後，不需經過繁瑣的打包步驟，請依序執行以下即可：

1. **安裝 Python 依賴包 (僅需執行一次)**
   請開啟終端機（或命令提示字元），進入程式碼目錄後執行：
   ```bash
   pip install -r requirements.txt
   ```
   > ⚠️ 如果你在公司內網遇到 `pip` 安裝逾時，請諮詢公司 IT 設定 proxy `--proxy=http://你的PROXY位址`。

2. **啟動伺服器**
   在專案根目錄下，連按兩下（或於終端機執行）`start.bat`。

3. **開始使用**
   不要關閉跳出的黑色終端機視窗，開啟瀏覽器連接：
   > [http://localhost:8000](http://localhost:8000)

只要完成以上三個簡單步驟，即可馬上看到完整的 AI SOP 對話視窗、串聯各種 MD 檔案並且支援即時串流回答與路由功能！

---

## 💡 常見問題與除錯 (Q&A)

### Q1: 啟動後看到命令提示字元有亂碼？
A: 我們的 `start.bat` 預設已經內建 `chcp 65001` 及強制的環境變數 `PYTHONUTF8` 去解決 UTF-8 中文不支援的問題了。如果您仍遇到無法對齊的亂碼，可以嘗試在使用舊版 Windows「命令提示字元」時，改變字型為 Consolas 系列。

### Q2: 執行 `start.bat` 閃退？
A: 這個原因通常出在電腦沒安裝 Python，導致沒抓到執行環境；又或者是 Port `8000` 已經被別的伺服器佔用。請將終端開啟並用打字的方式執行 `start.bat` 觀看錯誤代碼，藉此找出真正原因。
